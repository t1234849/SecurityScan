# 📌 版本兼容性说明

## ✅ 问题已修复

你遇到的插件兼容性问题已经解决！

### 🐛 原问题

```
Plugin is not compatible with the current version of the IDE, 
because it requires build 232 or newer but the current build is IC-231.9225.16
```

**原因：**
- 插件要求最低版本：**232**（IDEA 2023.2）
- 你的 IDEA 版本：**231**（IDEA 2023.1）
- 版本不匹配，无法安装

### ✅ 修复方案

我已经修改了 `build.gradle.kts` 中的版本配置：

**修改前：**
```kotlin
intellij {
    version.set("2023.2.5")  // 开发时使用的版本
}

patchPluginXml {
    sinceBuild.set("232")    // 最低支持 2023.2
    untilBuild.set("242.*")  // 最高支持 2024.2
}
```

**修改后：**
```kotlin
intellij {
    version.set("2022.2.5")  // 降低基础版本
}

patchPluginXml {
    sinceBuild.set("222")    // 最低支持 2022.2
    untilBuild.set("243.*")  // 最高支持 2024.3
}
```

---

## 🎯 现在支持的版本

### IDEA 版本对照表

| Build 版本 | IDEA 版本 | 是否支持 |
|-----------|-----------|---------|
| 222 | 2022.2 | ✅ 支持 |
| 223 | 2022.3 | ✅ 支持 |
| 231 | 2023.1 | ✅ 支持（你的版本）|
| 232 | 2023.2 | ✅ 支持 |
| 233 | 2023.3 | ✅ 支持 |
| 241 | 2024.1 | ✅ 支持 |
| 242 | 2024.2 | ✅ 支持 |
| 243 | 2024.3 | ✅ 支持 |

**支持范围：IDEA 2022.2 - 2024.3**

### 各版本类型

插件支持所有 IntelliJ 产品：
- ✅ **IC** (IntelliJ IDEA Community)
- ✅ **IU** (IntelliJ IDEA Ultimate)

---

## 🔄 重新构建插件

### 步骤 1：清理旧构建

```cmd
gradlew.bat clean
```

### 步骤 2：重新构建

```cmd
gradlew.bat buildPlugin
```

### 步骤 3：安装新插件

```cmd
# 新插件位置不变
build\distributions\SecurityScan-1.0-SNAPSHOT.zip
```

在 IDEA 中：
1. `File` → `Settings` → `Plugins`
2. 如果之前安装过，先卸载旧版本
3. 点击 ⚙️ → `Install Plugin from Disk...`
4. 选择新构建的 zip 文件
5. 重启 IDEA

---

## 🎯 验证兼容性

### 方法 1：查看插件信息

安装后在 IDEA 中：
1. `File` → `Settings` → `Plugins`
2. 找到"代码安全扫描助手"
3. 查看详情，应该显示：
   ```
   Version: 1.0-SNAPSHOT
   Vendor: SecurityScan Team
   Compatibility: 2022.2 - 2024.3
   ```

### 方法 2：检查 plugin.xml

打开构建后的 zip 文件，查看 `META-INF/plugin.xml`：

```xml
<idea-version since-build="222" until-build="243.*"/>
```

---

## 📊 版本兼容性策略

### 当前策略

```kotlin
sinceBuild.set("222")    // 最低版本：2022.2
untilBuild.set("243.*")  // 最高版本：2024.3.*
```

**优势：**
- ✅ 支持大部分常用 IDEA 版本
- ✅ 覆盖 2022.2 - 2024.3（2年半的版本范围）
- ✅ 兼容性好，用户群广

### 如何调整版本范围

如果需要支持更多版本，修改 `build.gradle.kts`：

**支持更老的版本：**
```kotlin
sinceBuild.set("213")  // 支持 2021.3 及以上
```

**支持更新的版本：**
```kotlin
untilBuild.set("251.*")  // 支持到未来版本
```

**支持所有版本（不推荐）：**
```kotlin
sinceBuild.set("213")
untilBuild.set("")  // 不设置上限
```

---

## 🔍 IDEA 版本号对照

### 常用版本

| 年份 | 版本 | Build 号 | 说明 |
|------|------|---------|------|
| 2021.3 | 2021.3.* | 213 | |
| 2022.1 | 2022.1.* | 221 | |
| 2022.2 | 2022.2.* | 222 | ← 当前插件最低支持 |
| 2022.3 | 2022.3.* | 223 | |
| 2023.1 | 2023.1.* | 231 | ← 你的版本 ✅ 现在支持 |
| 2023.2 | 2023.2.* | 232 | |
| 2023.3 | 2023.3.* | 233 | |
| 2024.1 | 2024.1.* | 241 | |
| 2024.2 | 2024.2.* | 242 | |
| 2024.3 | 2024.3.* | 243 | ← 当前插件最高支持 |

### 查看你的 IDEA 版本

在 IDEA 中：
1. `Help` → `About`
2. 查看版本信息：
   ```
   IntelliJ IDEA 2023.1.x
   Build #IC-231.9225.16
   ```

---

## 🚀 下一步操作

### 1. 重新构建插件

```cmd
# 清理旧构建
gradlew.bat clean

# 重新构建
gradlew.bat buildPlugin
```

### 2. 安装新插件

```cmd
# 插件位置
build\distributions\SecurityScan-1.0-SNAPSHOT.zip
```

在 IDEA 中安装：
1. `Settings` → `Plugins`
2. 卸载旧版本（如果有）
3. `Install Plugin from Disk...`
4. 选择新构建的 zip
5. 重启 IDEA

### 3. 验证安装

重启后：
1. `Settings` → `Editor` → `Inspections`
2. 展开 `Security` 分组
3. 应该能看到 9 个安全检查项

### 4. 测试功能

复制测试文件到你的项目：
```cmd
copy src\main\resources\examples\*.java <你的项目>\src\main\java\
```

打开测试文件，应该能看到黄色波浪线提示。

---

## 💡 常见问题

### Q: 如果还是报版本不兼容？

**可能原因：**
1. 没有重新构建插件（使用的是旧版本）
2. IDEA 版本太老（低于 2022.2）

**解决方法：**
```cmd
# 1. 清理
gradlew.bat clean

# 2. 删除旧缓存
rmdir /s /q build

# 3. 重新构建
gradlew.bat buildPlugin

# 4. 重新安装
```

### Q: 如何支持更老的 IDEA 版本？

修改 `build.gradle.kts`：

```kotlin
patchPluginXml {
    sinceBuild.set("213")  // 支持 2021.3
    untilBuild.set("243.*")
}
```

然后重新构建。

### Q: 如何支持最新的 IDEA 版本？

修改 `build.gradle.kts`：

```kotlin
patchPluginXml {
    sinceBuild.set("222")
    untilBuild.set("")  // 不设置上限，支持所有未来版本
}
```

---

## 🎉 总结

- ✅ **已修复**：现在支持 IDEA 2022.2 - 2024.3
- ✅ **你的版本**：2023.1 (build 231) 现在可以使用
- ✅ **操作步骤**：重新构建 → 重新安装 → 重启 IDEA
- ✅ **测试方法**：打开测试文件查看黄色波浪线

---

<div align="center">

**重新构建后即可在你的 IDEA 2023.1 中使用插件！🎉**

</div>

